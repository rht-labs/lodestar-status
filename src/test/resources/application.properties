# open api
quarkus.swagger-ui.always-include=true

# version configuration
version.yml=src/test/resources/data/version/version-manifest.yaml
main.version.key=ball
git.commit=3k2mek3
git.tag=development

# disable for test
quarkus.scheduler.enabled=false